{
  "name": "electerm-ios",
  "version": "4.15.157",
  "private": true,
  "type": "module",
  "description": "electerm for iOS — a free and open-source ssh/sftp/telnet/RDP/VNC/Spice/ftp client, built with Capacitor + a Node.js runtime.",
  "scripts": {
    "build": "node build.mjs",
    "overlay": "node build.mjs --overlay-only",
    "sync": "cap sync ios && node build.mjs --overlay-only",
    "ios": "npm run build && (cap add ios || true); cap sync ios && node build.mjs --overlay-only"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capawesome/capacitor-nodejs": "^0.1.0",
    "typescript": "^5.7.0"
  }
}
